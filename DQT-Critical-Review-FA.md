# بازبینی نقادانه اسناد DQT

**تاریخ:** ۱۵ اوت ۲۰۲۶ · **دامنه:** ۱۰ سند بارگذاری‌شده در Space پروژه‌ی DQT
**روش:** تحلیل تناقض‌های درونی + راستی‌آزمایی مستقل مخزن `alisadeghiaghili/data-quality-toolkit` روی شاخه‌ی `main` + راستی‌آزمایی ادعاهای بیرونی با وب

---

## خلاصه‌ی مدیریتی

سه مشکل ساختاری وجود دارد که تک‌تکِ ایرادهای بعدی از آن‌ها زاییده می‌شوند:

1. **یک قانون، سه نسخه.** `instruction.md`، `SKILL-dqt-architect.md` و `CONVENTIONS-DQT.md` هر سه همان قواعد را تعریف می‌کنند و همین حالا با هم واگرا شده‌اند (مجموعه‌ی ابعاد کیفیت داده در هر سه فرق دارد). این «drift» اجتناب‌ناپذیر است، نه اتفاقی.
2. **سند وضعیت، وضعیت را اشتباه گزارش می‌کند.** `DQT_Space_Summary_Complete.md` خودش را «ground truth، نه aspirational» معرفی می‌کند، اما دست‌کم **۵ ادعای مشخص آن امروز روی `main` نادرست است** — از جمله سه مورد از پنج آیتم P0. تصمیم‌گیری بر مبنای این سند، تصمیم‌گیری بر مبنای داده‌ی غلط است.
3. **اسناد یک تعارض ایمنی را ندیده‌اند.** «cleansing برگشت‌پذیر» + «اتصال read-only» + «cleanse در `run()`» سه الزامی هستند که در کنار هم قابل تحقق نیستند و کد فعلی دقیقاً روی لبه‌ی این تعارض ایستاده است.

**اولویت پیشنهادی:** اول تعارض ایمنی cleansing (BLOCKER)، بعد یکسان‌سازی منبع حقیقت، بعد تصحیح سند وضعیت. بازنویسی ماتریس‌ها و مراجع در آخر.

---

## ۱. مسائل سطح BLOCKER

### B-1 · «cleansing برگشت‌پذیر» با «اتصال read-only» جمع‌ناپذیر است و هیچ سندی این را نمی‌بیند

**سندها:** `CONVENTIONS-DQT.md` §C3 · `CONVENTIONS-DQT-data-model.md` §4 · `instruction.md` · هر دو SKILL

سه الزام همزمان و متناقض:

- `data-model` §۴: «DQT باید با نقش **read-only** کار کند: فقط SELECT، بدون DELETE/UPDATE/DDL روی داده‌ی production.»
- `CONVENTIONS` §C3: cleansing باید «استانداردسازی، حذف تکراری، تصحیح مبتنی بر lookup» انجام دهد و «برگشت‌پذیر» باشد.
- `CONVENTIONS` §A1: `cleanse` یکی از مراحل `DQTPipeline` است.

آنچه در کد واقعی است (راستی‌آزمایی‌شده):

- `src/dqt/sql/cleansing.py` مستقیماً `UPDATE`/`DELETE` روی جدول مبدأ اجرا می‌کند، `commit()` می‌زند و به دسترسی نوشتن نیاز دارد.
- «برگشت‌پذیری» در آن یعنی: «لاگ اطلاعات کافی برای undo کردنِ **دستیِ** هر تغییر را دارد.» این برگشت‌پذیری نیست؛ این مستندسازی خسارت است.
- در `pipeline.py`، `run()` متد `self.cleanse(result)` را **بی‌قید و شرط** صدا می‌زند. فقط به این دلیل امروز خطرناک نیست که `cleanse()` فعلاً pass-through است.

**چرا BLOCKER:** آیتم P0 شماره ۳ و «قدم بعدی» شماره ۴ در Space Summary دقیقاً می‌گوید «cleanse را به `run()` وصل کن». اجرای همان توصیه، بدون تغییر طراحی، به این معناست که **اجرای پیش‌فرضِ pipeline روی دیتابیس production بنویسد** — بدون dry-run، بدون flag، بدون rollback خودکار، و در تناقض مستقیم با قرارداد امنیتی خودِ پروژه. این یک باگ مستندات است که به یک حادثه تبدیل می‌شود.

**اصلاح اعمال‌شده در اسناد بازنویسی‌شده:**

- مدل دو اتصالی: `read_connection` (پیش‌فرض، فقط SELECT) و `write_connection` (اختیاری، صریح). تمام مراحل جز cleansing حق استفاده از `write_connection` را ندارند.
- `cleansing.mode: plan | apply` با پیش‌فرض `plan`. حالت `plan` فقط اسکریپت SQL و `CleansingLog` تولید می‌کند و چیزی اجرا نمی‌کند.
- `cleanse` از مسیر پیش‌فرض `run()` **حذف** و به یک نقطه‌ی ورود جداگانه منتقل شد.
- تعریف «برگشت‌پذیر» سفت شد: هر عملیات باید `undo_statement` قابل اجرا تولید کند، وگرنه حق ندارد خودش را reversible بنامد.

---

### B-2 · فایل‌های rules عملاً کد اجرایی‌اند، ولی هیچ سندی مدل تهدید ندارد

**سندها:** `CONVENTIONS-DQT.md` §C2 · `CONVENTIONS-DQT-data-model.md` §2.3 · `dqt_competitors.md` §1.1

`RuleConfig.expression: str` به‌عنوان «DSL یا قطعه‌ی SQL» تعریف شده و فایل‌های YAML قاعده از مسیر `rule_files` بارگذاری می‌شوند. بخش «Security Conventions» فقط درباره‌ی نشت DSN حرف می‌زند و یک کلمه درباره‌ی این ندارد که **یک فایل YAML قاعده، از نظر سطح دسترسی، معادل اجرای کد دلخواه روی دیتابیس است**.

در کد فعلی:

- شناسه‌ها (نام schema/table/column) با f-string مستقیماً داخل SQL درج می‌شوند.
- مقادیر `range` هم مستقیم درج می‌شوند: `f'"{column_name}" < {min_val}'` — و `min_val` از فایل YAML می‌آید. این یک بردار تزریق کامل است، نه فرضی.
- فقط `regex` پارامتری شده است.

خبر خوب: در نسخه‌ی فعلی نوع قاعده‌ی «raw SQL» وجود ندارد و expressionهای ناشناخته رد می‌شوند. یعنی هنوز فرصت هست که این را قبل از باز شدنِ درِ SQL دلخواه ببندید. سند فعلی این در را باز می‌گذارد.

**اصلاح اعمال‌شده:** بخش صریح Threat Model اضافه شد؛ الزام `quote_identifier()` به‌ازای هر dialect، الزام bound parameter برای **تمام** مقادیر (شامل `range`)، و ممنوعیت افزودن نوع قاعده‌ی raw SQL تا زمانی که allowlist تعریف شود.

---

## ۲. مسائل سطح MAJOR

### M-1 · Space Summary در ۵ نقطه‌ی مشخص با واقعیت مخزن نمی‌خواند

سند خودش را «Ground Truth, Not Aspirational» می‌نامد. راستی‌آزمایی مستقل روی `main`:

| ادعای سند | واقعیت روی `main` | اثر |
|---|---|---|
| P0 #1: فایل تکراری `.github/workflows/ci.yml` وجود دارد | این فایل **404** است؛ وجود ندارد | آیتم P0 شماره ۱ و «قدم بعدی شماره ۱» هر دو منتفی‌اند |
| P0 #3: مرحله‌ی `cleanse` در `run()` صدا زده نمی‌شود | صدا زده **می‌شود** (`result = self.cleanse(result)`)؛ ولی خودِ تابع pass-through است | تشخیص درست است، توصیف غلط — و همین تفاوت، B-1 را پنهان کرده |
| §7: بسته‌ی موازی `src/data_quality_toolkit/` وجود دارد و «تعارض هویتی حل‌نشده» است | در فهرست ریشه‌ی مخزن، `src/` به‌صورت `src/dqt/` نمایش داده می‌شود؛ GitHub وقتی یک پوشه تنها یک فرزند دارد مسیر را ادغام می‌کند. یعنی `src/` احتمالاً فقط `dqt/` را دارد | «بزرگ‌ترین ریسک محصول» سند، احتمالاً دیگر وجود ندارد |
| §5: تست یتیم `tests/test_visualization_heatmap.py` | **404** است | استدلال «کد مخفی در بسته‌ی legacy» بی‌پایه می‌شود |
| CI با ماتریس Python **۳.۹–۳.۱۳** | CI فقط `3.11` و `3.12` است و `pyproject` می‌گوید `requires-python >= 3.11` | ادعای بی‌پایه |

**ریشه‌ی متدولوژیک:** بخش عمده‌ی «وضعیت پیاده‌سازی» سند بر مبنای **حجم فایل به بایت** استنتاج شده («۲.۸ کیلوبایت، پس نازک است»، «۲۳.۴ کیلوبایت، پس پیاده‌سازی شده»). حجم فایل معیار عملکرد نیست. اتفاقاً در دو مورد این حدس درست از آب درآمد (diagnostics واقعاً فقط completeness دارد، monitoring واقعاً pass-through است) و همین درست‌بودنِ تصادفی، اعتماد کاذب به روش ایجاد کرده است.

**اصلاح:** سند وضعیت با ستون «شواهد» بازنویسی شد و هر ردیف یا به یک فایل/تابع خوانده‌شده ارجاع می‌دهد یا صراحتاً `UNVERIFIED` علامت خورده است.

---

### M-2 · مجموعه‌ی ابعاد کیفیت داده در چهار سند، چهار جور است

| سند | ابعاد اعلام‌شده |
|---|---|
| `instruction.md` | completeness, consistency, validity, uniqueness, referential integrity — **بدون timeliness** |
| `SKILL-dqt-architect.md` | completeness, consistency, validity, uniqueness, timeliness, referential integrity |
| `CONVENTIONS-DQT.md` §C1 | completeness, consistency, validity, uniqueness, timeliness — **بدون referential integrity** |
| `dqt_competitors.md` §1.1 | هر شش مورد |
| `data-model` §1.5 | «مثلاً completeness, validity, uniqueness» — یعنی هیچ مجموعه‌ی بسته‌ای |

و در `data-model`، `dimension: str` است — رشته‌ی آزاد، بدون enum و بدون CHECK در طرح ذخیره‌سازی. نتیجه‌ی عملی: دو run با املای متفاوت (`referential_integrity` در برابر `referential integrity`) دو سری زمانی مجزا می‌سازند و trend analysis در monitoring بی‌صدا خراب می‌شود. این ایراد فقط تا زمانی بی‌ضرر است که monitoring واقعی نشده باشد.

**اصلاح:** یک enum بسته‌ی شش‌تایی snake_case به‌عنوان تنها منبع حقیقت، با CHECK constraint در ذخیره‌سازی و export از `dqt.__init__`.

**نکته‌ی جانبی درباره‌ی timeliness:** هیچ سندی نگفته timeliness چطور محاسبه می‌شود. برای این بُعد باید ستون زمانی مرجع پیکربندی شود؛ در غیاب آن، بُعد باید **skip** شود نه اینکه امتیاز ۱.۰ بگیرد. سکوت فعلیِ اسناد، طراحی را به سمت گزینه‌ی دوم هل می‌دهد که یعنی امتیاز کیفیتِ دروغین.

---

### M-3 · سطح API عمومیِ الزامی، تا حد زیادی خیالی است

`CONVENTIONS` §A3 و SKILL معماری هر دو الزام می‌کنند این‌ها export شوند:
`DQTPipeline`, `PipelineResult`, `RuleSet`, `DQIssue`, `DQMetrics`, `DomainConfig`, `ClassificationResult` به‌علاوه‌ی `from_sqlalchemy_engine()`.

واقعیت `src/dqt/__init__.py` (خوانده‌شده):

- `RuleSet`, `DomainConfig`, `ClassificationResult` **وجود ندارند** — چون knowledge/classification اصلاً نوشته نشده‌اند.
- `DQMetrics` وجود ندارد؛ نامِ واقعی `DQMetric` (مفرد) است. یعنی سند قرارداد، نام کلاسِ خودش را در `data-model` غلط نوشته.
- `from_sqlalchemy_engine()` وجود ندارد — و **SQLAlchemy اصلاً در وابستگی‌های `pyproject.toml` نیست.** پروژه مستقیم با DBAPI2 کار می‌کند. یعنی این الزام نه فقط پیاده نشده، بلکه با معماری واقعی ناسازگار است.
- در عوض، ۲۳ نماد export شده که سند اصلاً نمی‌شناسدشان: از جمله `RuleScope` و `SamplingConfig` — همان دو تایپی که `data-model` به آن‌ها ارجاع می‌دهد ولی **هرگز تعریفشان نمی‌کند** (این خودش یک ایراد مستقل است: «single source of truth» که دو تایپ ارجاع‌شده‌اش را تعریف نکرده).

تصمیم درستی که در جلسه‌ی قبل گرفته شده بود — «exportهای جعلی نساز» — درست بود، ولی سند قرارداد هرگز به‌روزرسانی نشد. الان سند و کد در حال جنگ‌اند.

**اصلاح:** §A3 بازنویسی شد به «سطح فعلی» + «سطح هدف، مشروط به وجود تایپ»؛ `DQMetrics` → `DQMetric` در همه‌جا؛ `from_sqlalchemy_engine()` به بخش «رد شده، با دلیل» منتقل شد؛ `RuleScope` و `SamplingConfig` در `data-model` تعریف شدند.

---

### M-4 · مدل داده، قانون تجمیع ندارد ⇒ ریسک شمارش مضاعف

`PipelineResult` هم `issues` و `metrics` سراسری دارد، هم `SchemaResult.issues`، هم `TableResult.issues`، هم `ColumnResult.issues`. سند هیچ‌جا نمی‌گوید کدام‌یک canonical است.

دو تفسیر ممکن، هر دو سازگار با متن فعلی:
- `PipelineResult.issues` = اجتماع همه ⇒ جمع‌زدن سطوح، دوباره‌شماری است.
- `PipelineResult.issues` = فقط issueهای بدون scope ⇒ گزارش سراسری، مسائل جدول‌ها را از قلم می‌اندازد.

هر پیاده‌سازیِ گزارش/UI که این را حدس بزند، نصف احتمال دارد اشتباه حدس بزند. برای سندی که خودش را «single source of truth» می‌نامد، این یک نقص جدی است.

**اصلاح:** `PipelineResult.issues` و `.metrics` به‌عنوان تنها لیست canonical و مسطح تعریف شدند؛ مجموعه‌های تودرتو صراحتاً «view غیرمرجع» اعلام شدند و **جمع‌زدنشان ممنوع** شد.

---

### M-5 · طرح ذخیره‌سازی، صفحه‌های الزامیِ UI را پشتیبانی نمی‌کند

SKILL طراح UI صفحه‌ی «Rules & Checks Management» را الزامی می‌کند، با «تاریخچه‌ی نتایج قواعد در طول زمان». طرح ذخیره‌سازی در `data-model` §۳ فقط سه جدول دارد: `runs`, `run_metrics`, `run_issues`. **هیچ جدولی برای `RuleResult` / `RuleRunResult` وجود ندارد.** یعنی یک صفحه‌ی الزامیِ UI روی داده‌ای ساخته شده که هرگز ذخیره نمی‌شود.

ایرادهای فرعی همان طرح:

- بدون FOREIGN KEY از `run_metrics`/`run_issues` به `runs` ⇒ رکورد یتیم.
- بدون `created_at`.
- بدون جدول `connections` با اینکه `connection_id` همه‌جا کلید خارجیِ ضمنی است.
- `dimension TEXT` بدون CHECK (نگاه کنید به M-2).
- بدون UNIQUE روی `(run_id, schema, table, column, dimension)` — فقط index معمولی پیشنهاد شده؛ یعنی درج تکراری مجاز است.
- بدون جدول برای `CleansingLog` — با اینکه «audit trail» یک الزام صریح C3 است. audit trailی که persist نشود، audit trail نیست.

**اصلاح:** طرح با `connections`, `run_rule_results`, `cleansing_log` تکمیل و FK/UNIQUE/CHECK/`created_at` اضافه شد.

---

### M-6 · ادعای پشتیبانی چند-dialect بی‌پشتوانه است

`CONVENTIONS` §C1 و SKILL معماری از «Postgres/MySQL/SQL Server» حرف می‌زنند. واقعیت:

- `pyproject.toml` فقط یک extra دیتابیسی دارد: `postgres`. هیچ درایور MySQL یا SQL Server نیست.
- `schema_discovery.py` فقط SQLite و PostgreSQL را می‌شناسد.
- تست‌های integration در CI فقط روی SQLite اجرا می‌شوند — در حالی که `CONVENTIONS` §T1 صراحتاً «dockerized Postgres/MySQL/SQLite» را الزام کرده. یعنی CI موجود، الزام سند خودش را برآورده نمی‌کند و کسی متوجه نشده.
- هیچ لایه‌ی انتزاع dialect وجود ندارد؛ SQL با f-string و به‌ازای هر درایور جداگانه ساخته می‌شود. افزودن dialect سوم یعنی سه‌برابر شدن مسیرهای کد، نه افزودن یک adapter.

ضمناً extra مربوط به postgres **هم `psycopg[binary]>=3.1` و هم `psycopg2-binary>=2.9`** را نصب می‌کند — دو درایور موازی برای یک دیتابیس. یکی باید حذف شود.

**اصلاح:** ماتریس صریح dialect اضافه شد (SQLite = dev/test، PostgreSQL = هدف اصلی v0.1، MySQL/SQL Server = خارج از v0.1)؛ الزام لایه‌ی `dialects/` قبل از افزودن دیتابیس سوم؛ الزام حذف درایور تکراری.

---

### M-7 · وضعیت `PipelineResult.status` هرگز نمی‌تواند `failed` یا `partial` شود

`data-model` §1.1 سه وضعیت تعریف می‌کند: `success`, `failed`, `partial`. در `run()` واقعی، `result.status = "success"` هاردکد شده و هیچ `try/except` سطح-مرحله‌ای وجود ندارد. اگر مرحله‌ای شکست بخورد، exception بالا می‌رود و **هیچ رکوردی persist نمی‌شود** — یعنی جدول `runs` هرگز ردیفی با `status='failed'` نخواهد داشت.

نتیجه: هر داشبورد یا هشداری که روی «runهای ناموفق» ساخته شود، همیشه صفر برمی‌گرداند. این بدترین نوع باگ است: خطای خاموش که شبیه سلامت به نظر می‌رسد.

**اصلاح:** الزام ایزوله‌سازی خطا در هر مرحله، فیلد `stage_errors`، و معناشناسی صریح سه وضعیت به `data-model` اضافه شد.

---

## ۳. مسائل سطح MINOR (ولی واقعی)

### m-1 · هر سه سند «قانون»، یک قانون را تعریف می‌کنند

`instruction.md` تقریباً بازنویسی `SKILL-dqt-architect.md` است و آن هم بازنویسی `CONVENTIONS-DQT.md`. سه نسخه یعنی سه مسیر واگرایی — و M-2 مدرک عملیِ همین است. یکی باید canonical باشد و دو تای دیگر فقط ارجاع بدهند.

**اصلاح:** `CONVENTIONS-DQT.md` در مخزن canonical شد؛ SKILLها و `instruction.md` به رفتار و مسیریابی تقلیل یافتند و قواعد دامنه را تکرار نمی‌کنند.

### m-2 · چک‌باکس‌های `CONVENTIONS` صددرصد کهنه‌اند

Status Legend سه حالت دارد ولی **تمام** آیتم‌ها `[ ] TODO` هستند، در حالی که rules engine، cleansing، pipeline، CI، گزارش HTML و API عمومی همه پیاده شده‌اند. سندی که وضعیتش هرگز به‌روز نمی‌شود، بدتر از سندی است که اصلاً وضعیت ندارد — چون فعالانه گمراه می‌کند.

### m-3 · `[web:22]` و `[cite:89]` نشانه‌های مرده‌اند

این مارکرها در سراسر `CONVENTIONS`، `dqt_competitors` و `DQT UI Ecosystem` پخش‌اند و به هیچ چیزی اشاره نمی‌کنند. داخل مخزن Git، اینها صرفاً نویزند و توهم استناد ایجاد می‌کنند. یا لینک واقعی، یا حذف.

### m-4 · ستون «Uses missingly» در ماتریس اکوسیستم، معیار نیست

این ستون در `dqt_ecosystem.md` به‌عنوان یک facet آمده، ولی طبق تعریف فقط DQT در آن `✓✓` می‌گیرد و بقیه `-`. این یک ستونِ خودتأییدکننده است که ماتریس را از ابزار سنجش به ابزار تبلیغ تبدیل می‌کند. اگر می‌خواهید بمانَد، باید به «Extensibility / pluggable external analyzers» تعمیم یابد تا واقعاً قابل رقابت باشد.

مشکل دوم همان ماتریس: ردیف **DQT (target)** در کنار ردیف‌های وضعیت **فعلیِ** رقبا نشسته است. مقایسه‌ی آرزو با واقعیت. باید دو ردیف شود: `DQT (current)` و `DQT (target v1.0)`.

مشکل سوم: به `missingly` در ستون‌های Profiling و DQ Diagnostics امتیاز `✓✓` داده شده. اگر `missingly` در تشخیص کیفیت داده قوی است، پس مرز «DQT = کیفیت داده، missingly = فقدان داده» — که کل معماری bridge روی آن بنا شده — مخدوش است. یا امتیاز اشتباه است یا مرزبندی.

### m-5 · تعریف «floor» با «floor برآورده‌شده» خلط شده

`dqt_competitors.md` §1.1 یک «baseline feature floor» تعریف می‌کند که شامل monitoring، knowledge، classification و گزارش PDF است. هیچ‌کدام وجود ندارند. سند این را نمی‌گوید. یک floor که هیچ‌وقت با وضعیت واقعی مقایسه نمی‌شود، فقط یک لیست آرزوست.

ضمناً در §2.6 برای MobyDQ شاخص «latency» به‌عنوان یکی از indicatorها آمده، در حالی که non-goal صریح پروژه «no latency» است. اینجا منظور latencyِ داده (تازگی) است نه latencyِ سرویس — ولی سند این تمایز را نمی‌گذارد و دقیقاً همان scope creepی را دعوت می‌کند که قرار بود جلویش را بگیرد.

### m-6 · گزارش دوزبانه EN/FA بدون هیچ الزام فنی

«گزارش HTML/PDF دوزبانه» در چهار سند تکرار شده، ولی هیچ‌جا به این اشاره نشده که فارسی نیازمند RTL، shaping خط عربی و **جاسازی فونت** در PDF است. هیچ وابستگی تولید PDF (WeasyPrint/ReportLab) در `pyproject.toml` نیست. این یعنی الزامی که نه ابزارش انتخاب شده نه سختی‌اش برآورد شده.

### m-7 · تناقض آستانه‌ی پوشش تست

`pyproject.toml` و CI هر دو `--cov-fail-under=80` دارند. Space Summary §۱۰ از یک «test plan با gate پوشش ۹۰٪» حرف می‌زند. یکی باید برنده شود.

### m-8 · `black` در قرارداد هست، در پروژه نیست

`CONVENTIONS` §T3 و `instruction.md` هر دو `ruff`/`black` را الزام می‌کنند. پروژه فقط `ruff` + `ruff format` دارد و `black` در dev-dependencies نیست. این تصمیم درستی است (ruff format جایگزین black است) ولی سند به‌روز نشده.

---

## ۴. راستی‌آزمایی ادعاهای بیرونی

| ادعای سند | یافته | اقدام |
|---|---|---|
| Apache Griffin به‌عنوان رقیب زنده و «north star» ماتریس | **بازنشسته شد (سپتامبر ۲۰۲۵)** و در نوامبر ۲۰۲۵ به Apache Attic منتقل شد؛ همه‌ی منابع read-only | به «مرجع تاریخی» منتقل شد، نه بنچمارک فعال |
| Talend Open Studio (DQ) به‌عنوان رقیب متن‌باز | نسخه‌ی متن‌باز Talend Studio **از ۳۱ ژانویه ۲۰۲۴ بازنشسته** شد؛ Talend اکنون بخشی از Qlik و تجاری است | برچسب «تجاری / OSS بازنشسته» اضافه شد |
| DataLens به‌عنوان یک محصول واحد | **دو محصول متفاوت با یک نام با هم قاطی شده‌اند.** توصیف «داشبورد از widgetها با cross-filtering، selector و parameter» مربوط به **Yandex DataLens** (ابزار BI ابری) است، نه به مقاله‌ی arXiv 2501.17074 که یک prototype پژوهشیِ کیفیت داده است. کل بخش «الگوهایی که از DataLens قرض می‌گیریم» روی یک ابزار ترکیبیِ ناموجود بنا شده | دو مدخل جدا شدند؛ الگوهای BI از الزامات UI حذف شدند |
| MobyDQ به‌عنوان رقیب فعال | مخزن `ubisoft/mobydq` عمومی و بایگانی‌نشده است، اما نشانه‌ای از توسعه‌ی فعال دیده نشد (تأیید تاریخ آخرین commit به‌دلیل robots.txt ممکن نشد) | `UNVERIFIED` علامت خورد |
| Baselinr | **واقعی است** — پلتفرم متن‌باز کیفیت داده و observability برای انبارهای SQL (`baselinr.io`، کپی‌رایت ۲۰۲۵). نزدیک‌ترین رقیب مستقیم DQT | نگه داشته شد، با هشدار «پروژه‌ی نوپا» |
| لینک‌های Great Expectations به مسیر `docs/0.18/` | آن صفحات صراحتاً می‌گویند «۰.۱۸.۲۱ دیگر نگهداری نمی‌شود»؛ نسخه‌ی پایدار فعلی **۱.۲۰.۰** است و مفاهیم (از جمله Data Docs) تغییر کرده‌اند | به مسیر latest به‌روزرسانی شد |
| لینک‌های Pydantic | معتبرند؛ resolve می‌شوند | بدون تغییر |
| pydqc | مخزن موجود است ولی نشانه‌ی نگهداری فعال ندارد | `UNVERIFIED` |

**جمع‌بندی این بخش:** از ۸ رقیبِ نام‌برده در `dqt_competitors.md`، دو تا بازنشسته‌اند و یکی اصلاً وجود خارجی به آن شکل ندارد. ماتریسی که DQT را با آن‌ها می‌سنجد، دارد با ارواح می‌سنجد.

---

## ۵. نقد بر خودِ SKILLها

فراتر از تکرار قواعد (m-1):

- **مرز مسیریابی ندارند.** یک درخواست «صفحه‌ی Rules Management را طراحی کن» هم به architect می‌خورد هم به ui-designer. هیچ قاعده‌ی تقدمی وجود ندارد.
- **خروجی قابل‌بررسی نمی‌خواهند.** هر دو می‌گویند «باید نقادانه باشی» ولی هیچ چک‌لیست یا فرمت خروجی الزامی تعریف نمی‌کنند. نتیجه‌ی عملی همین اسناد است: نقد هست، ولی هیچ‌کس مجبور نبوده ادعاها را به شواهد گره بزند — و دقیقاً به همین دلیل M-1 اتفاق افتاده.
- **`viz.py` را در چیدمان الزامی ماژول‌ها آورده‌اند** بدون هیچ کتابخانه‌ی رسمی نمودار در وابستگی‌ها. تصمیمِ «Plotly یا Matplotlib» گرفته نشده ولی ماژولش الزامی شده.
- **SKILL طراح UI هیچ الزام دسترسی‌پذیری ندارد** — و در همان حال «سبز/زرد/قرمز» را تنها حامل معنای severity قرار می‌دهد. برای کاربر دارای کوررنگی، این UI خواناییِ صفر دارد. الزام «رنگ + آیکون/متن» اضافه شد.
- **SKILL طراح UI به RTL اشاره نمی‌کند** با اینکه محصول برای DBAهای فارسی‌زبان است و گزارش‌هایش قرار است دوزبانه باشد.

---

## ۶. آنچه در این اسناد خوب است (و باید حفظ شود)

نقد بالا نباید این‌ها را بپوشاند:

- **مرزبندی scope واقعاً محکم است.** «no service monitoring / no masking / no MDM» در همه‌ی اسناد ثابت مانده و هیچ‌جا نقض نشده. این نادر است.
- **تصمیم «export جعلی نساز»** (حذف `RuleSet`/`DomainConfig`/`ClassificationResult` از API عمومی چون تایپشان وجود ندارد) یک تصمیم مهندسی بالغ است.
- **stub بودن `monitoring.py` صادقانه اعلام شده** و docstring آن دلیلش را توضیح می‌دهد — این خیلی بهتر از پیاده‌سازیِ ظاهریِ توخالی است.
- **بخش «ارزیابی صادقانه»ی Space Summary** لحن درستی دارد؛ مشکلش روش است نه صداقت.
- **مدل facetها** واقعاً کار می‌کند و جلوی scope creep را گرفته است.

---

## ۷. ترتیب اقدام پیشنهادی

1. **B-1** — مدل ایمنی cleansing را در سند قفل کنید **قبل از** اینکه کسی cleansing را به pipeline وصل کند. (این تنها موردی است که تأخیرش هزینه‌ی برگشت‌ناپذیر دارد.)
2. **B-2** — Threat Model فایل‌های قاعده + پارامتری‌کردن مقادیر `range`.
3. **M-2 / M-3** — enum ابعاد و سطح API را قفل کنید؛ این دو، پیش‌نیاز درست شدن ذخیره‌سازی و گزارش‌اند.
4. **M-1** — Space Summary را با شواهد بازنویسی کنید و آیتم‌های P0 منتفی‌شده را حذف کنید. (سه مورد از پنج مورد، کارِ الکی است.)
5. **M-4 / M-5 / M-7** — مدل داده و ذخیره‌سازی.
6. **M-6** — ماتریس dialect و حذف درایور تکراری.
7. بقیه‌ی MINORها هنگام لمس بعدیِ هر سند.

---

## منابع راستی‌آزمایی

- [Apache Attic — Griffin](https://attic.apache.org/projects/griffin.html)
- [Apache Attic — Retired Projects](https://attic.apache.org/projects.html)
- [Qlik — Talend Open Studio retirement](https://www.qlik.com/us/products/talend-open-studio)
- [arXiv 2501.17074 — DataLens (research prototype)](https://arxiv.org/abs/2501.17074)
- [Yandex Cloud — DataLens dashboard concepts](https://yandex.cloud/en/docs/datalens/concepts/dashboard)
- [Baselinr](https://baselinr.io/)
- [Great Expectations 0.18 docs (marked unmaintained)](https://docs.greatexpectations.io/docs/0.18/core/introduction/introduction/)
- [ubisoft/mobydq](https://github.com/ubisoft/mobydq)
- [alisadeghiaghili/data-quality-toolkit](https://github.com/alisadeghiaghili/data-quality-toolkit)
